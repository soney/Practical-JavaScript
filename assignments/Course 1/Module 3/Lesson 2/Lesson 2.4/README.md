# Problem 4: Arithmetic Operators for Score Calculations

Edit `Lesson 2.4/main-4.js`:

- Note 1: For this problem, you will **only** edit the JavaScript file, `Lesson 2.4/main-4.js`. **Do not modify the HTML file**, `Lesson 2.4/index.html`.
- Note 2: The testing/export block at the bottom is boilerplate for the grader. Add your code above that block. Do not modify or remove the testing/export block; the tests use that block to access your variables and functions.
- Note 3: The tests check your variable names and values, not whether you use `let` or `const`. None of these variables need to be reassigned, so `const` is a good choice. `let` is also accepted if the final values are correct.
- Note 4: When a step says to log a value **to the console**, do it by calling `console.log(...)`. For example, `console.log(totalScore);` prints `totalScore` to the console.

1. **Declare variables**:
   - Create `score1` with value `45`
   - Create `score2` with value `55`
   - Create `score3` with value `50`

2. **Perform calculations**:
   - Create a variable `totalScore` that adds all three scores. Log it to the console.
   - Create a variable `averageScore` that calculates the average of the three scores. Log it to the console.
   - Create a variable `negativeScore` that stores the negation of `score1`. Log it to the console.
   - Create a variable `averageSentence` that says: `"The average of 45, 55, and 50 is 50."`
     - Use `score1`, `score2`, `score3`, and `averageScore` in the sentence. Log it to the console.

---

Course 1, Module 3 - practice assignment (ungraded): [Practice: Types and Expressions](https://www.coursera.org/learn/web-development-fundamentals-html-css-javascript/programming/vNxVP/practice-types-and-expressions) - `Lesson 2.4`

The files here are the starter you get in the course. [`solution/main-4.js`](solution/main-4.js) is the finished `main-4.js`; copy it over the starter to run the completed assignment.
