# Problem 1: CSS Rules for Headings and Text

Edit `Lesson 1.1/index.html`:

The starter file already has an empty `<style>` element inside the `<head>` section. Add your CSS rules inside that `<style>` element.

Do not change the HTML content inside the `<body>`, and do not use inline `style` attributes.

Add CSS rules with these selectors, properties, and values:

1. For the `h1` selector:
   - Set `font-size` to `30px`.
   - Set `font-weight` to `bold`.
2. For the `div` selector:
   - Set `background-color` to `navy`.
   - Set `padding` to `10px`.
3. For the `p` selector:
   - Set `color` to `white`.
   - Set `font-size` to `16px`.

The resulting page should look similar to this image:

![Expected output: styled heading, a navy content section, and white paragraph text](layout.png)

---

Course 1, Module 2 - practice assignment (ungraded): [Practice: Adding CSS](https://www.coursera.org/learn/web-development-fundamentals-html-css-javascript/programming/OzFCf/practice-adding-css) - `Lesson 1.1`

The files here are the starter you get in the course. The finished `index.html` is in the [solution folder on GitHub](https://github.com/soney/Practical-JavaScript/tree/main/assignments/Course%201/Module%202/Lesson%201/Lesson%201.1/solution); in the course codespace that folder is hidden so you can work the problem first.
