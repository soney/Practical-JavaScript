// Add the `export` keyword to each function so that other modules can import it.

function celsiusToFahrenheit(celsius) {
    // TODO: add the export keyword to this function and return celsius * 9 / 5 + 32
}

function fahrenheitToCelsius(fahrenheit) {
    // TODO: add the export keyword to this function and return (fahrenheit - 32) * 5 / 9
}
