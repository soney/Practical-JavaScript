// Edit this file to install and use the lodash library.
//
// A browser cannot resolve `import _ from 'lodash'` on its own. Once lodash is
// installed in node_modules, webpack finds it and bundles it into main.js when
// the project is built.

// TODO: import the installed lodash library as `_` (see the problem description).

const scores = [70, 92, 70, 85, 92, 61];

// TODO: use lodash to compute:
//   - the unique scores, with `_.uniq`
//   - those unique scores sorted from lowest to highest, with `_.sortBy`
//   - the highest score, with `_.max`
// Then render them inside the element with ID `app`, using the exact markup
// shown in the problem description.
