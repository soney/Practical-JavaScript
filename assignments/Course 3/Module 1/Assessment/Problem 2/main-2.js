// TODO: Add three import statements at the top of this file.
// 1. A named import of `cToF` from './converters.js'.
// 2. A namespace import of './converters.js' bound to the name `converters`.
// 3. The default import from './formatter.js' bound to the name `formatTemperature`.

// TODO: Fill in the Fahrenheit paragraph.
// Convert 100 Celsius with the named import `cToF`, format it with the unit 'F',
// and assign the result to the textContent of the element with ID `fahrenheit`.

// TODO: Fill in the Celsius paragraph.
// Convert 32 Fahrenheit with `converters.fToC` (the namespace import), format it
// with the unit 'C', and assign the result to the textContent of the element with
// ID `celsius`.
