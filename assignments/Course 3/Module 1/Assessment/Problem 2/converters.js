// Turn each function into a NAMED export by adding the `export` keyword,
// then fill in its body.

function cToF(c) {
    // TODO: add the export keyword to this function and return c * 9 / 5 + 32
}

function fToC(f) {
    // TODO: add the export keyword to this function and return (f - 32) * 5 / 9
}
