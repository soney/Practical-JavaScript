// Make this function the DEFAULT export of the module, then fill in its body.

function formatTemperature(value, unit) {
    // TODO: make this the default export (export default) and return value + a degree symbol + unit
}
