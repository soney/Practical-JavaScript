// This file uses Lodash, which index.html loads from a CDN <script> tag, so the
// Lodash global `_` is available here. In a project with a bundler you would
// import it instead:
//   import _ from "lodash";
// There is no build step here (Lodash is pre-packaged from a CDN), so we use the
// global.

const prices = [12, 5, 20, 8, 15];

// TODO: Use Lodash to compute the total of all prices with _.sum(prices),
// then render it into the element with ID total:
//   document.querySelector('#total').textContent = String(value);

// TODO: Use Lodash to compute the average of the prices with _.mean(prices),
// then render it into the element with ID average.

// TODO: Use Lodash to find the largest price with _.max(prices),
// then render it into the element with ID largest.
