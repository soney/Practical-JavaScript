// Edit this file.
//
// The lodash package has already been installed for you. Running
//     npm install lodash
// added lodash to the "dependencies" in package.json and downloaded it into a
// node_modules folder.
//
// To use an installed package, import it by NAME (with no "./"). This is
// different from loading your own files, which use a relative path like
// import result from './main.js'.

// TODO: (1) import the lodash package (its default export) and store it in a
// constant named _ .

const names = ['ada', 'grace', 'alan', 'katherine'];

// TODO: (2) replace the empty array below. Use lodash to capitalize each name by
// setting capitalizedNames to _.map(names, _.capitalize).
const capitalizedNames = [];

export default capitalizedNames;
