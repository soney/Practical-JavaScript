// The #display element reference is provided for you.
const display = document.querySelector("#display");

// TODO: write your code below (follow steps 1-2): define resolveAfter(value, ms), then call it and .then() the resolved value into #display.
//
// 1. Define a function resolveAfter(value, ms) that returns a Promise
//    which resolves to `value` after `ms` milliseconds.
//
// 2. Call resolveAfter with a value and a delay, then use .then() to
//    show the resolved value inside #display.
